# Hugging Face Spaces installs this file before app.py runs. Keep the private
# GitHub dependency out of build-time resolution; app.py installs stable-audio-3
# with GITHUB_TOKEN when the Space starts.
# ZeroGPU can assign Blackwell sm_120 hardware. PyTorch 2.8.0 is the
# oldest ZeroGPU-supported release whose PyPI wheels use CUDA 12.8 packages.
torch==2.8.0
torchaudio==2.8.0
gradio>=5.0.0
huggingface_hub>=0.36.0
numpy>=2.2.6
soundfile>=0.13.1
safetensors>=0.7.0
einops>=0.8.2
PyYAML>=6.0.2
