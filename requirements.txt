# Hugging Face Spaces installs this file before app.py runs. Keep the private
# GitHub dependency out of build-time resolution; app.py installs stable-audio-3
# with GITHUB_TOKEN when the Space starts.
# ZeroGPU can assign Blackwell sm_120 hardware; use PyTorch's CUDA 12.8 wheels
# because the default PyPI CUDA 12.6 build only carries kernels through sm_90.
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.7.1+cu128
torchaudio==2.7.1+cu128
gradio>=5.0.0
huggingface_hub>=0.36.0
numpy>=2.2.6
soundfile>=0.13.1
safetensors>=0.7.0
einops>=0.8.2
PyYAML>=6.0.2
